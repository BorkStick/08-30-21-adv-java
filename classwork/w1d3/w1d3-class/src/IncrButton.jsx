import React from 'react'

export default function IncrButton(props) {
    const handleIncrButton=(props)=>{}
    return(
      <button onClick={props.handleIncrButton}>Increase Count</button>
    )
}
